# 字典是无序的数据结合，所以print输出无序，
# 通常和定义的顺序是不一致的
xiaoming = {"name":"小明",
            "age":18,
            "gender":True,
            "height":1.75,
            "weight":50}
# 为了阅读清晰，定义时每一个键值对都各自占有一行代码
print(xiaoming)